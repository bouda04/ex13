<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.testsql.MainActivity" >

    <GridView
        android:id="@+id/gridView1"
        android:layout_width="match_parent"
        android:layout_height="335dp"
        android:layout_alignLeft="@+id/textView1"
        android:background="@android:drawable/dialog_holo_light_frame"
        android:numColumns="4" >

    </GridView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/gridView1"
 		android:layout_below="@+id/gridView1"
 		android:layout_marginTop="20dp"
        android:background="@android:drawable/dialog_holo_light_frame">

        <Button
            android:id="@+id/btOrderNum"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="reArrangeItems"
            android:text="Sort by Numbers" />

        <Button
            android:id="@+id/btOrderColor"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="reArrangeItems"
            android:text="Sort by Colors" />

        <Button
            android:id="@+id/btShuffle"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="reArrangeItems"
            android:text="Shuffle" />

    </LinearLayout>

</RelativeLayout>